<style id='resume-style-inline-css' type='text/css'>

/* Color Scheme */

{% if theme_config.current_preset != 'custom' %}

/* Background Color */
body {
  background-color: {{ site.presets[theme_config.current_preset].background_color }};
}

/* Sidebar Background Color */
body:before,
.site-header {
  background-color: {{ site.presets[theme_config.current_preset].header_background_color }};
}

/* Box Background Color */
.post-navigation,
.pagination,
.secondary,
.site-footer,
.hentry,
.page-header,
.page-content,
.comments-area,
.widecolumn {
  background-color: {{ site.presets[theme_config.current_preset].box_background_color }};
}

/* Box Background Color */
button,
input[type="button"],
input[type="reset"],
input[type="submit"],
.pagination .prev,
.pagination .next,
.widget_calendar tbody a,
.widget_calendar tbody a:hover,
.widget_calendar tbody a:focus,
.page-links a,
.page-links a:hover,
.page-links a:focus,
.sticky-post {
  color: {{ site.presets[theme_config.current_preset].box_background_color }};
}

/* Main Text Color */
button,
input[type="button"],
input[type="reset"],
input[type="submit"],
.pagination .prev,
.pagination .next,
.widget_calendar tbody a,
.page-links a,
.sticky-post {
  background-color: {{ site.presets[theme_config.current_preset].textcolor }};
}

/* Main Text Color */
body,
blockquote cite,
blockquote small,
a,
.dropdown-toggle:after,
.image-navigation a:hover,
.image-navigation a:focus,
.comment-navigation a:hover,
.comment-navigation a:focus,
.widget-title,
.entry-footer a:hover,
.entry-footer a:focus,
.comment-metadata a:hover,
.comment-metadata a:focus,
.pingback .edit-link a:hover,
.pingback .edit-link a:focus,
.comment-list .reply a:hover,
.comment-list .reply a:focus,
.site-info a:hover,
.site-info a:focus {
  color: {{ site.presets[theme_config.current_preset].textcolor }};
}

/* Main Text Color */
.entry-content a,
.entry-summary a,
.page-content a,
.comment-content a,
.pingback .comment-body > a,
.author-description a,
.taxonomy-description a,
.textwidget a,
.entry-footer a:hover,
.comment-metadata a:hover,
.pingback .edit-link a:hover,
.comment-list .reply a:hover,
.site-info a:hover {
  border-color: {{ site.presets[theme_config.current_preset].textcolor }};
}

/* Secondary Text Color */
button:hover,
button:focus,
input[type="button"]:hover,
input[type="button"]:focus,
input[type="reset"]:hover,
input[type="reset"]:focus,
input[type="submit"]:hover,
input[type="submit"]:focus,
.pagination .prev:hover,
.pagination .prev:focus,
.pagination .next:hover,
.pagination .next:focus,
.widget_calendar tbody a:hover,
.widget_calendar tbody a:focus,
.page-links a:hover,
.page-links a:focus {
  background-color: {{ site.presets[theme_config.current_preset].textcolor }}; /* Fallback for IE7 and IE8 */
  background-color: {{ site.presets[theme_config.current_preset].secondary_textcolor }};
}

/* Secondary Text Color */
blockquote,
a:hover,
a:focus,
.main-navigation .menu-item-description,
.post-navigation .meta-nav,
.post-navigation a:hover .post-title,
.post-navigation a:focus .post-title,
.image-navigation,
.image-navigation a,
.comment-navigation,
.comment-navigation a,
.widget,
.author-heading,
.entry-footer,
.entry-footer a,
.taxonomy-description,
.page-links > .page-links-title,
.entry-caption,
.comment-author,
.comment-metadata,
.comment-metadata a,
.pingback .edit-link,
.pingback .edit-link a,
.post-password-form label,
.comment-form label,
.comment-notes,
.comment-awaiting-moderation,
.logged-in-as,
.form-allowed-tags,
.no-comments,
.site-info,
.site-info a,
.wp-caption-text,
.gallery-caption,
.comment-list .reply a,
.widecolumn label,
.widecolumn .mu_register label {
  color: {{ site.presets[theme_config.current_preset].textcolor }}; /* Fallback for IE7 and IE8 */
  color: {{ site.presets[theme_config.current_preset].secondary_textcolor }};
}

/* Secondary Text Color */
blockquote,
.logged-in-as a:hover,
.comment-author a:hover {
  border-color: {{ site.presets[theme_config.current_preset].textcolor }}; /* Fallback for IE7 and IE8 */
  border-color: {{ site.presets[theme_config.current_preset].secondary_textcolor }};
}

/* Border Color */
hr,
.dropdown-toggle:hover,
.dropdown-toggle:focus {
  background-color: {{ site.presets[theme_config.current_preset].textcolor }}; /* Fallback for IE7 and IE8 */
  background-color: {{ site.presets[theme_config.current_preset].border_color }};
}

/* Border Color */
pre,
abbr[title],
table,
th,
td,
input,
textarea,
.main-navigation ul,
.main-navigation li,
.post-navigation,
.post-navigation div + div,
.pagination,
.comment-navigation,
.widget li,
.widget_categories .children,
.widget_nav_menu .sub-menu,
.widget_pages .children,
.site-header,
.site-footer,
.hentry + .hentry,
.author-info,
.entry-content .page-links a,
.page-links > span,
.page-header,
.comments-area,
.comment-list + .comment-respond,
.comment-list article,
.comment-list .pingback,
.comment-list .trackback,
.comment-list .reply a,
.no-comments {
  border-color: {{ site.presets[theme_config.current_preset].textcolor }}; /* Fallback for IE7 and IE8 */
  border-color: {{ site.presets[theme_config.current_preset].border_color }};
}

/* Border Focus Color */
a:focus,
button:focus,
input:focus {
  outline-color: {{ site.presets[theme_config.current_preset].textcolor }}; /* Fallback for IE7 and IE8 */
  outline-color: {{ site.presets[theme_config.current_preset].border_focus_color }};
}

input:focus,
textarea:focus {
  border-color: {{ site.presets[theme_config.current_preset].textcolor }}; /* Fallback for IE7 and IE8 */
  border-color: {{ site.presets[theme_config.current_preset].border_focus_color }};
}

/* Sidebar Link Color */
.secondary-toggle:before {
  color: {{ site.presets[theme_config.current_preset].sidebar_textcolor }};
}

.site-title a,
.site-description {
  color: {{ site.presets[theme_config.current_preset].sidebar_textcolor }};
}

/* Sidebar Text Color */
.site-title a:hover,
.site-title a:focus {
  color: {{ site.presets[theme_config.current_preset].secondary_sidebar_textcolor }};
}

/* Sidebar Border Color */
.secondary-toggle {
  border-color: {{ site.presets[theme_config.current_preset].sidebar_textcolor }}; /* Fallback for IE7 and IE8 */
  border-color: {{ site.presets[theme_config.current_preset].sidebar_border_color }};
}

/* Sidebar Border Focus Color */
.secondary-toggle:hover,
.secondary-toggle:focus {
  border-color: {{ site.presets[theme_config.current_preset].sidebar_textcolor }}; /* Fallback for IE7 and IE8 */
  border-color: {{ site.presets[theme_config.current_preset].sidebar_border_focus_color }};
}

.site-title a {
  outline-color: {{ site.presets[theme_config.current_preset].sidebar_textcolor }}; /* Fallback for IE7 and IE8 */
  outline-color: {{ site.presets[theme_config.current_preset].sidebar_border_focus_color }};
}

/* Meta Background Color */
.entry-footer {
  background-color: {{ site.presets[theme_config.current_preset].meta_box_background_color }};
}

@media screen and (min-width: 38.75em) {
  /* Main Text Color */
  .page-header {
    border-color: {{ site.presets[theme_config.current_preset].textcolor }};
  }
}

@media screen and (min-width: 59.6875em) {
  /* Make sure its transparent on desktop */
  .site-header,
  .secondary {
    background-color: transparent;
  }

  /* Sidebar Background Color */
  .widget button,
  .widget input[type="button"],
  .widget input[type="reset"],
  .widget input[type="submit"],
  .widget_calendar tbody a,
  .widget_calendar tbody a:hover,
  .widget_calendar tbody a:focus {
    color: {{ site.presets[theme_config.current_preset].header_background_color }};
  }

  /* Sidebar Link Color */
  .secondary a,
  .dropdown-toggle:after,
  .widget-title,
  .widget blockquote cite,
  .widget blockquote small {
    color: {{ site.presets[theme_config.current_preset].sidebar_textcolor }};
  }

  .widget button,
  .widget input[type="button"],
  .widget input[type="reset"],
  .widget input[type="submit"],
  .widget_calendar tbody a {
    background-color: {{ site.presets[theme_config.current_preset].sidebar_textcolor }};
  }

  .textwidget a {
    border-color: {{ site.presets[theme_config.current_preset].sidebar_textcolor }};
  }

  /* Sidebar Text Color */
  .secondary a:hover,
  .secondary a:focus,
  .main-navigation .menu-item-description,
  .widget,
  .widget blockquote,
  .widget .wp-caption-text,
  .widget .gallery-caption {
    color: {{ site.presets[theme_config.current_preset].secondary_sidebar_textcolor }};
  }

  .widget button:hover,
  .widget button:focus,
  .widget input[type="button"]:hover,
  .widget input[type="button"]:focus,
  .widget input[type="reset"]:hover,
  .widget input[type="reset"]:focus,
  .widget input[type="submit"]:hover,
  .widget input[type="submit"]:focus,
  .widget_calendar tbody a:hover,
  .widget_calendar tbody a:focus {
    background-color: {{ site.presets[theme_config.current_preset].secondary_sidebar_textcolor }};
  }

  .widget blockquote {
    border-color: {{ site.presets[theme_config.current_preset].secondary_sidebar_textcolor }};
  }

  /* Sidebar Border Color */
  .main-navigation ul,
  .main-navigation li,
  .widget input,
  .widget textarea,
  .widget table,
  .widget th,
  .widget td,
  .widget pre,
  .widget li,
  .widget_categories .children,
  .widget_nav_menu .sub-menu,
  .widget_pages .children,
  .widget abbr[title] {
    border-color: {{ site.presets[theme_config.current_preset].sidebar_border_color }};
  }

  .dropdown-toggle:hover,
  .dropdown-toggle:focus,
  .widget hr {
    background-color: {{ site.presets[theme_config.current_preset].sidebar_border_color }};
  }

  .widget input:focus,
  .widget textarea:focus {
    border-color: {{ site.presets[theme_config.current_preset].sidebar_border_focus_color }};
  }

  .sidebar a:focus,
  .dropdown-toggle:focus {
    outline-color: {{ site.presets[theme_config.current_preset].sidebar_border_focus_color }};
  }
}

{% else %}

/* Background Color */
body {
  background-color: {{ theme_config.background_color }};
}

/* Sidebar Background Color */
body:before,
.site-header {
  background-color: {{ theme_config.header_background_color }};
}

/* Box Background Color */
.post-navigation,
.pagination,
.secondary,
.site-footer,
.hentry,
.page-header,
.page-content,
.comments-area,
.widecolumn {
  background-color: {{ theme_config.box_background_color }};
}

/* Box Background Color */
button,
input[type="button"],
input[type="reset"],
input[type="submit"],
.pagination .prev,
.pagination .next,
.widget_calendar tbody a,
.widget_calendar tbody a:hover,
.widget_calendar tbody a:focus,
.page-links a,
.page-links a:hover,
.page-links a:focus,
.sticky-post {
  color: {{ theme_config.box_background_color }};
}

/* Main Text Color */
button,
input[type="button"],
input[type="reset"],
input[type="submit"],
.pagination .prev,
.pagination .next,
.widget_calendar tbody a,
.page-links a,
.sticky-post {
  background-color: {{ theme_config.textcolor }};
}

/* Main Text Color */
body,
blockquote cite,
blockquote small,
a,
.dropdown-toggle:after,
.image-navigation a:hover,
.image-navigation a:focus,
.comment-navigation a:hover,
.comment-navigation a:focus,
.widget-title,
.entry-footer a:hover,
.entry-footer a:focus,
.comment-metadata a:hover,
.comment-metadata a:focus,
.pingback .edit-link a:hover,
.pingback .edit-link a:focus,
.comment-list .reply a:hover,
.comment-list .reply a:focus,
.site-info a:hover,
.site-info a:focus {
  color: {{ theme_config.textcolor }};
}

/* Main Text Color */
.entry-content a,
.entry-summary a,
.page-content a,
.comment-content a,
.pingback .comment-body > a,
.author-description a,
.taxonomy-description a,
.textwidget a,
.entry-footer a:hover,
.comment-metadata a:hover,
.pingback .edit-link a:hover,
.comment-list .reply a:hover,
.site-info a:hover {
  border-color: {{ theme_config.textcolor }};
}

/* Secondary Text Color */
button:hover,
button:focus,
input[type="button"]:hover,
input[type="button"]:focus,
input[type="reset"]:hover,
input[type="reset"]:focus,
input[type="submit"]:hover,
input[type="submit"]:focus,
.pagination .prev:hover,
.pagination .prev:focus,
.pagination .next:hover,
.pagination .next:focus,
.widget_calendar tbody a:hover,
.widget_calendar tbody a:focus,
.page-links a:hover,
.page-links a:focus {
  background-color: {{ theme_config.textcolor }}; /* Fallback for IE7 and IE8 */
  background-color: {{ theme_config.secondary_textcolor }};
}

/* Secondary Text Color */
blockquote,
a:hover,
a:focus,
.main-navigation .menu-item-description,
.post-navigation .meta-nav,
.post-navigation a:hover .post-title,
.post-navigation a:focus .post-title,
.image-navigation,
.image-navigation a,
.comment-navigation,
.comment-navigation a,
.widget,
.author-heading,
.entry-footer,
.entry-footer a,
.taxonomy-description,
.page-links > .page-links-title,
.entry-caption,
.comment-author,
.comment-metadata,
.comment-metadata a,
.pingback .edit-link,
.pingback .edit-link a,
.post-password-form label,
.comment-form label,
.comment-notes,
.comment-awaiting-moderation,
.logged-in-as,
.form-allowed-tags,
.no-comments,
.site-info,
.site-info a,
.wp-caption-text,
.gallery-caption,
.comment-list .reply a,
.widecolumn label,
.widecolumn .mu_register label {
  color: {{ theme_config.textcolor }}; /* Fallback for IE7 and IE8 */
  color: {{ theme_config.secondary_textcolor }};
}

/* Secondary Text Color */
blockquote,
.logged-in-as a:hover,
.comment-author a:hover {
  border-color: {{ theme_config.textcolor }}; /* Fallback for IE7 and IE8 */
  border-color: {{ theme_config.secondary_textcolor }};
}

/* Border Color */
hr,
.dropdown-toggle:hover,
.dropdown-toggle:focus {
  background-color: {{ theme_config.textcolor }}; /* Fallback for IE7 and IE8 */
  background-color: {{ theme_config.border_color }};
}

/* Border Color */
pre,
abbr[title],
table,
th,
td,
input,
textarea,
.main-navigation ul,
.main-navigation li,
.post-navigation,
.post-navigation div + div,
.pagination,
.comment-navigation,
.widget li,
.widget_categories .children,
.widget_nav_menu .sub-menu,
.widget_pages .children,
.site-header,
.site-footer,
.hentry + .hentry,
.author-info,
.entry-content .page-links a,
.page-links > span,
.page-header,
.comments-area,
.comment-list + .comment-respond,
.comment-list article,
.comment-list .pingback,
.comment-list .trackback,
.comment-list .reply a,
.no-comments {
  border-color: {{ theme_config.textcolor }}; /* Fallback for IE7 and IE8 */
  border-color: {{ theme_config.border_color }};
}

/* Border Focus Color */
a:focus,
button:focus,
input:focus {
  outline-color: {{ theme_config.textcolor }}; /* Fallback for IE7 and IE8 */
  outline-color: {{ theme_config.border_focus_color }};
}

input:focus,
textarea:focus {
  border-color: {{ theme_config.textcolor }}; /* Fallback for IE7 and IE8 */
  border-color: {{ theme_config.border_focus_color }};
}

/* Sidebar Link Color */
.secondary-toggle:before {
  color: {{ theme_config.sidebar_textcolor }};
}

.site-title a,
.site-description {
  color: {{ theme_config.sidebar_textcolor }};
}

/* Sidebar Text Color */
.site-title a:hover,
.site-title a:focus {
  color: {{ theme_config.secondary_sidebar_textcolor }};
}

/* Sidebar Border Color */
.secondary-toggle {
  border-color: {{ theme_config.sidebar_textcolor }}; /* Fallback for IE7 and IE8 */
  border-color: {{ theme_config.sidebar_border_color }};
}

/* Sidebar Border Focus Color */
.secondary-toggle:hover,
.secondary-toggle:focus {
  border-color: {{ theme_config.sidebar_textcolor }}; /* Fallback for IE7 and IE8 */
  border-color: {{ theme_config.sidebar_border_focus_color }};
}

.site-title a {
  outline-color: {{ theme_config.sidebar_textcolor }}; /* Fallback for IE7 and IE8 */
  outline-color: {{ theme_config.sidebar_border_focus_color }};
}

/* Meta Background Color */
.entry-footer {
  background-color: {{ theme_config.meta_box_background_color }};
}

@media screen and (min-width: 38.75em) {
  /* Main Text Color */
  .page-header {
    border-color: {{ theme_config.textcolor }};
  }
}

@media screen and (min-width: 59.6875em) {
  /* Make sure its transparent on desktop */
  .site-header,
  .secondary {
    background-color: transparent;
  }

  /* Sidebar Background Color */
  .widget button,
  .widget input[type="button"],
  .widget input[type="reset"],
  .widget input[type="submit"],
  .widget_calendar tbody a,
  .widget_calendar tbody a:hover,
  .widget_calendar tbody a:focus {
    color: {{ theme_config.header_background_color }};
  }

  /* Sidebar Link Color */
  .secondary a,
  .dropdown-toggle:after,
  .widget-title,
  .widget blockquote cite,
  .widget blockquote small {
    color: {{ theme_config.sidebar_textcolor }};
  }

  .widget button,
  .widget input[type="button"],
  .widget input[type="reset"],
  .widget input[type="submit"],
  .widget_calendar tbody a {
    background-color: {{ theme_config.sidebar_textcolor }};
  }

  .textwidget a {
    border-color: {{ theme_config.sidebar_textcolor }};
  }

  /* Sidebar Text Color */
  .secondary a:hover,
  .secondary a:focus,
  .main-navigation .menu-item-description,
  .widget,
  .widget blockquote,
  .widget .wp-caption-text,
  .widget .gallery-caption {
    color: {{ theme_config.secondary_sidebar_textcolor }};
  }

  .widget button:hover,
  .widget button:focus,
  .widget input[type="button"]:hover,
  .widget input[type="button"]:focus,
  .widget input[type="reset"]:hover,
  .widget input[type="reset"]:focus,
  .widget input[type="submit"]:hover,
  .widget input[type="submit"]:focus,
  .widget_calendar tbody a:hover,
  .widget_calendar tbody a:focus {
    background-color: {{ theme_config.secondary_sidebar_textcolor }};
  }

  .widget blockquote {
    border-color: {{ theme_config.secondary_sidebar_textcolor }};
  }

  /* Sidebar Border Color */
  .main-navigation ul,
  .main-navigation li,
  .widget input,
  .widget textarea,
  .widget table,
  .widget th,
  .widget td,
  .widget pre,
  .widget li,
  .widget_categories .children,
  .widget_nav_menu .sub-menu,
  .widget_pages .children,
  .widget abbr[title] {
    border-color: {{ theme_config.sidebar_border_color }};
  }

  .dropdown-toggle:hover,
  .dropdown-toggle:focus,
  .widget hr {
    background-color: {{ theme_config.sidebar_border_color }};
  }

  .widget input:focus,
  .widget textarea:focus {
    border-color: {{ theme_config.sidebar_border_focus_color }};
  }

  .sidebar a:focus,
  .dropdown-toggle:focus {
    outline-color: {{ theme_config.sidebar_border_focus_color }};
  }
}

{% endif %}

</style>