<div id="header">
    <div class="row">
        <div class="large-3 columns">
            {% if site.title %}
                <h2>{{ site.title }}</h2>
            {% endif %}   
            {% if site.description %}
                <h6 class="light">{{ site.description }}</h6>
            {% endif %}
        </div>
        <div class="large-3 columns">
            <div class="row">
                <div class="small-4 columns light italic">
                    Address:
                </div>
                <div class="small-8 columns border-left light ">
                    <ul class="no-bullet">
                        {% for item in site.address %}
                            <li>{{ item.line }}</li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
        <div class="large-3 columns">
            <div class="row">
                <div class="small-4 columns light italic">
                    Contact:
                </div>
                <div class="small-8 columns border-left light">
                    <ul class="no-bullet">
                        {% for item in site.contact %}
                            <li>{{ item.line }}</li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
        <div class="large-2 columns text-right italic website">
            {{ site.website }}
        </div>
    </div>
</div>